sv_maxclients {{maxClients}}
{{serverEndpoints}}
set sv_licenseKey "{{svLicense}}"
{{addPrincipalsMaster}}

set onesync on
set mysql_connection_string "{{dbConnectionString}}"

ensure oxmysql

ensure legacy-core
ensure legacy-analytics

ensure legacy-multichar
ensure legacy-apartments
ensure legacy-spawn
ensure legacy-world
ensure legacy-smallresources

ensure legacy-menu
ensure legacy-input
ensure legacy-radial
ensure legacy-target

ensure legacy-public
